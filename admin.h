//#ifndef ADMIN_H
#define ADMIN_H
#include <iostr//eam>
#include <string>
#include "Client.h"
#include "Compte.h"

class admin
{
public:
	admin();
	~admin();
	procte:
		friend admin prod(admin, Client);
		friend admin prod(admin, Compte);
	
};

#endif